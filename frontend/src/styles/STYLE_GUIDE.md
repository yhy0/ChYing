# Vue组件样式管理规范

## 📋 目录

1. [概述](#概述)
2. [样式架构](#样式架构)
3. [命名规范](#命名规范)
4. [组件样式开发指南](#组件样式开发指南)
5. [CSS变量使用规范](#css变量使用规范)
6. [响应式设计规范](#响应式设计规范)
7. [可访问性要求](#可访问性要求)
8. [性能优化指南](#性能优化指南)
9. [常见问题解答](#常见问题解答)

## 🎯 概述

本项目采用基于UnoCSS的样式架构，结合玻璃态设计风格，通过CSS变量和模块化组织实现样式统一管理。本规范旨在确保代码的一致性、可维护性和可扩展性。

### 核心原则

- **统一性**：所有组件使用统一的样式系统
- **模块化**：样式按功能和组件分类管理
- **可复用**：避免重复代码，提高开发效率
- **可维护**：清晰的命名和组织结构
- **性能优先**：优化CSS加载和渲染性能

## 🏗️ 样式架构

### 目录结构

```
frontend/src/styles/
├── index.css                 # 主入口文件
├── variables.css             # CSS变量定义
├── STYLE_GUIDE.md           # 样式规范文档
├── components/              # 组件样式
│   ├── button.css           # 按钮组件
│   ├── card.css             # 卡片组件
│   ├── form.css             # 表单组件
│   ├── table.css            # 表格组件
│   ├── color-picker.css     # 颜色选择器
│   ├── editor-panel.css     # 编辑器面板
│   └── utilities.css        # 工具类样式
└── modules/                 # 模块样式
    ├── plugins.css          # 插件系统
    ├── proxy.css            # 代理模块
    └── ...                  # 其他模块
```

### 导入顺序

样式文件按以下顺序导入，确保正确的层叠关系：

1. **基础样式**：变量、字体、工具类
2. **布局组件**：布局、卡片、滚动条
3. **表单交互**：按钮、表单、搜索
4. **状态反馈**：状态、通知、徽章
5. **数据展示**：表格、标签、编辑器
6. **UI组件**：对话框、菜单、面板
7. **模块样式**：特定功能模块

## 📝 命名规范

### CSS类命名

采用BEM（Block Element Modifier）命名方法论：

```css
/* 块（Block） */
.component-name { }

/* 元素（Element） */
.component-name__element { }

/* 修饰符（Modifier） */
.component-name--modifier { }
.component-name__element--modifier { }
```

### 实际示例

```css
/* ✅ 正确示例 */
.color-picker { }                    /* 颜色选择器块 */
.color-picker__option { }            /* 颜色选项元素 */
.color-picker__option--active { }    /* 激活状态修饰符 */
.color-picker--compact { }           /* 紧凑模式修饰符 */

/* ❌ 错误示例 */
.colorPicker { }                     /* 避免驼峰命名 */
.color_picker { }                    /* 避免下划线 */
.cp-option { }                       /* 避免缩写 */
```

### CSS变量命名

```css
/* 颜色变量 */
--color-primary                      /* 主色 */
--color-text-primary                 /* 主要文本色 */
--color-bg-secondary                 /* 次要背景色 */

/* 尺寸变量 */
--size-button-height                 /* 按钮高度 */
--size-panel-width                   /* 面板宽度 */

/* 玻璃态变量 */
--glass-bg-primary                   /* 玻璃态主背景 */
--glass-border-light                 /* 玻璃态浅边框 */
--glass-shadow-medium                /* 玻璃态中等阴影 */
```

## 🎨 组件样式开发指南

### 新组件样式开发流程

1. **分析需求**：确定组件的视觉需求和交互状态
2. **检查复用**：查看是否有现有样式可以复用
3. **创建文件**：在适当目录创建样式文件
4. **定义变量**：使用CSS变量定义可配置属性
5. **编写样式**：遵循命名规范和架构原则
6. **添加导入**：在index.css中添加导入
7. **测试验证**：测试不同主题和响应式效果

### 组件样式模板

```css
/**
 * 组件名称样式
 * 组件功能描述
 */

/* ===== CSS变量定义 ===== */
:root {
  /* 组件特定变量 */
  --component-bg: var(--glass-bg-primary);
  --component-border: var(--glass-border);
  --component-text: var(--color-text-primary);
}

/* 深色模式变量 */
.dark {
  --component-bg: var(--glass-bg-secondary);
}

/* ===== 主要样式 ===== */
.component-name {
  background: var(--component-bg);
  border: 1px solid var(--component-border);
  color: var(--component-text);
  transition: var(--transition-fast);
}

/* ===== 元素样式 ===== */
.component-name__element {
  /* 元素样式 */
}

/* ===== 修饰符样式 ===== */
.component-name--modifier {
  /* 修饰符样式 */
}

/* ===== 响应式设计 ===== */
@media (max-width: 768px) {
  .component-name {
    /* 移动端适配 */
  }
}

/* ===== 可访问性增强 ===== */
.component-name:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* 高对比度模式支持 */
@media (prefers-contrast: high) {
  .component-name {
    border-width: 2px;
  }
}

/* 减少动画模式支持 */
@media (prefers-reduced-motion: reduce) {
  .component-name {
    transition: none;
  }
}
```

### Vue组件中的样式使用

```vue
<template>
  <div class="component-name" :class="{ 'component-name--active': isActive }">
    <div class="component-name__header">
      <h3 class="component-name__title">标题</h3>
    </div>
    <div class="component-name__content">
      内容区域
    </div>
  </div>
</template>

<!-- ✅ 推荐：样式已迁移到统一管理 -->
<!-- 样式已迁移到 styles/components/component-name.css 统一管理 -->

<!-- ❌ 避免：组件内定义样式 -->
<!-- 
<style scoped>
.component-name { }
</style>
-->
```

## 🎛️ CSS变量使用规范

### 变量层次结构

```css
/* 1. 全局基础变量 */
:root {
  --color-primary: #3b82f6;
  --font-family-base: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto;
}

/* 2. 语义化变量 */
:root {
  --color-text-primary: var(--color-gray-900);
  --color-bg-primary: var(--color-white);
}

/* 3. 组件变量 */
:root {
  --button-bg: var(--color-primary);
  --button-text: var(--color-white);
}

/* 4. 玻璃态变量 */
:root {
  --glass-bg-primary: rgba(248, 250, 252, 0.85);
  --glass-border: rgba(148, 163, 184, 0.8);
}
```

### 变量使用最佳实践

```css
/* ✅ 正确：使用语义化变量 */
.button {
  background: var(--button-bg);
  color: var(--button-text);
  border: 1px solid var(--button-border);
}

/* ❌ 错误：直接使用颜色值 */
.button {
  background: #3b82f6;
  color: #ffffff;
  border: 1px solid #e5e7eb;
}

/* ✅ 正确：提供回退值 */
.component {
  background: var(--component-bg, #ffffff);
}

/* ✅ 正确：使用计算值 */
.component {
  padding: calc(var(--spacing-base) * 2);
}
```

## 📱 响应式设计规范

### 断点定义

```css
/* 移动设备 */
@media (max-width: 480px) { }

/* 平板设备 */
@media (max-width: 768px) { }

/* 桌面设备 */
@media (max-width: 1024px) { }

/* 大屏设备 */
@media (min-width: 1025px) { }
```

### 响应式开发原则

1. **移动优先**：从小屏幕开始设计，逐步增强
2. **渐进增强**：确保基础功能在所有设备上可用
3. **内容优先**：优先保证内容的可读性和可访问性
4. **性能考虑**：避免在小屏幕上加载不必要的样式

### 响应式示例

```css
/* 基础样式（移动优先） */
.component {
  padding: 0.5rem;
  font-size: 0.875rem;
}

/* 平板适配 */
@media (min-width: 768px) {
  .component {
    padding: 1rem;
    font-size: 1rem;
  }
}

/* 桌面适配 */
@media (min-width: 1024px) {
  .component {
    padding: 1.5rem;
    font-size: 1.125rem;
  }
}
```

## ♿ 可访问性要求

### 必须遵循的可访问性标准

1. **颜色对比度**：文本与背景的对比度至少为4.5:1
2. **焦点指示**：所有可交互元素必须有清晰的焦点状态
3. **键盘导航**：确保所有功能都可以通过键盘访问
4. **屏幕阅读器**：提供适当的ARIA标签和语义化HTML

### 可访问性样式示例

```css
/* 焦点状态 */
.button:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* 高对比度模式 */
@media (prefers-contrast: high) {
  .component {
    border-width: 2px;
    background: var(--color-bg-high-contrast);
  }
}

/* 减少动画 */
@media (prefers-reduced-motion: reduce) {
  .component {
    transition: none;
    animation: none;
  }
}

/* 强制颜色模式 */
@media (forced-colors: active) {
  .component {
    border: 1px solid ButtonText;
  }
}
```

## ⚡ 性能优化指南

### CSS性能最佳实践

1. **避免深层嵌套**：CSS选择器嵌套不超过3层
2. **使用高效选择器**：避免使用通配符和复杂选择器
3. **减少重绘重排**：避免频繁修改布局属性
4. **合理使用动画**：优先使用transform和opacity

### 性能优化示例

```css
/* ✅ 高效选择器 */
.button { }
.button--primary { }
.button__icon { }

/* ❌ 低效选择器 */
div > ul > li > a { }
.container .sidebar .menu .item { }

/* ✅ 高性能动画 */
.component {
  transform: translateX(0);
  transition: transform 0.3s ease;
}

.component--moved {
  transform: translateX(100px);
}

/* ❌ 低性能动画 */
.component {
  left: 0;
  transition: left 0.3s ease;
}

.component--moved {
  left: 100px;
}
```

### 关键渲染路径优化

```css
/* 关键样式内联 */
.critical-component {
  display: flex;
  align-items: center;
  background: var(--glass-bg-primary);
}

/* 非关键样式延迟加载 */
@media (min-width: 1024px) {
  .enhanced-component {
    box-shadow: var(--glass-shadow-strong);
    backdrop-filter: var(--glass-blur-medium);
  }
}
```

## ❓ 常见问题解答

### Q: 什么时候应该创建新的样式文件？

A: 当满足以下条件之一时：
- 新组件有独特的样式需求
- 样式代码超过100行
- 多个组件需要共享相同的样式逻辑
- 现有文件已经过于庞大

### Q: 如何处理样式冲突？

A: 遵循以下优先级：
1. 使用更具体的选择器
2. 调整CSS导入顺序
3. 使用CSS变量覆盖
4. 必要时使用`!important`（谨慎使用）

### Q: 如何确保深色模式兼容性？

A: 
1. 所有颜色都使用CSS变量定义
2. 在`.dark`选择器下重新定义变量
3. 测试所有组件在两种模式下的表现
4. 使用相对亮度而非绝对颜色值

### Q: 如何优化玻璃态效果的性能？

A: 
1. 合理使用`backdrop-filter`，避免过度使用
2. 在低性能设备上提供降级方案
3. 使用`will-change`属性优化动画性能
4. 避免在滚动容器上使用复杂的玻璃态效果

### Q: 如何处理第三方组件的样式？

A: 
1. 优先使用组件提供的主题定制功能
2. 通过CSS变量覆盖样式
3. 创建专门的覆盖样式文件
4. 避免直接修改第三方组件的样式文件

## 📚 参考资源

- [CSS变量文档](https://developer.mozilla.org/zh-CN/docs/Web/CSS/Using_CSS_custom_properties)
- [BEM命名方法论](https://getbem.com/)
- [Web可访问性指南](https://www.w3.org/WAI/WCAG21/quickref/)
- [CSS性能优化](https://developer.mozilla.org/zh-CN/docs/Learn/Performance/CSS)

---

**最后更新时间**: 2024年12月

**维护者**: 前端开发团队

如有疑问或建议，请联系开发团队或在项目中提出Issue。